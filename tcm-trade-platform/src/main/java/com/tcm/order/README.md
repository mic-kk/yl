# order 业务域 — 订单履约（M4）

## 职责
全平台订单生命周期管理：待支付/待出库/已发货/已签收/已完成/售后；分批履约；订单取消回补。

## 约束
- 对外只暴露 Service 接口给其他业务域，禁止跨域访问 Mapper/Entity
- 履约指令走 integration 适配层（WMS/TMS/ERP），外部回调必须幂等

## 状态
脚手架阶段：空骨架，业务实现见后续迭代计划。
